// Tailwind CSS v4 uses a dedicated PostCSS plugin. Theme tokens live in
// src/app/globals.css via the CSS-first `@theme` block (no tailwind.config.ts).
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
