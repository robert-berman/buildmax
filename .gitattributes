# Normalize line endings to LF in the repo so the Linux Docker build and shell
# scripts are unaffected by Windows CRLF checkouts.
* text=auto eol=lf

# Treat generated data + lockfiles as text but skip diffs (large, machine-made).
package-lock.json linguist-generated=true -diff
src/data/generated/*.json linguist-generated=true -diff
